<% if @apps.empty? %>
	<p class="ink-vspace">No apps yet. <%= link_to 'Click here', new_app_path %> to create one.</p>
<% else %>
	<table>
		<thead>
			<th>App name</th>
			<th>Stack</th>
			<th>Internal IP</th>
			<th>Status</th>
		</thead>
		<tbody>
			<%= render @apps %>
		</tbody>
	</table>
<% end %>