<div class="ink-l100 mb">
	<h1>Your current apps</h1>
</div>

<% if @apps.empty? %>
	<p class="ink-vspace">No apps yet. <%= link_to 'Click here', new_app_path %> to create one.</p>
<% else %>
	<table class="ink-table ink-bordered ink-zebra ink-hover">
		<thead>
			<th>App name</th>
			<th>Stack</th>
			<th>Internal IP</th>
			<th>Port</th>
			<th>Status</th>
		</thead>
		<tbody>
			<%= render @apps %>
		</tbody>
	</table>
<% end %>